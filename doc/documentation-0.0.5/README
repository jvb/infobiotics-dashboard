To build the HTML documentation in '_build/html/': 
$ make html

To build the LaTeX documentation in '_build/latex/':
# replace index.rst with index_latex.rst for separate chapters in pdf
$ make latex
$ cd _build/latex
$ make

To build the man pages in 'man_pages/man_output/':
$ cd man_pages
$ ./generate_man
